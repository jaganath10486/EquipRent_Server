export enum ErrorTypes {
  INVALID_PAYLOAD = "Invalid Payload",
  PAYLOAD_NOT_AVAILABLE = "Payload Not Avilable",
  INTERNAL_SERVER_ERROR = "Internal Server Error",
}

export enum ErrorMessage {
  INTERNAL_SERVER_ERROR = "Internal Server Error",
}
