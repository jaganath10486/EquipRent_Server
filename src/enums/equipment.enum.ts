export enum PaymentMode {
  CASH = "cash",
  ONLINE = "online",
}

export enum EquipmentBookingStatus {
  PENDING = "pending",
  CONFIRMED = "confirmed",
  CANCELLED = "cancelled",
  PICKEDUP = "picked_up",
  DELIVERED = "delivered",
  RETURNED = "returned",
  OVERDUE = "overdue",
}

export enum DeliveryType {
  DELIVERY = "delivery",
  PICKUP = "pickup",
}
